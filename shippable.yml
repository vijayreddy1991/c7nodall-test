jobs:
  - name: c7nodall-testing
    type: runSh
    steps:
      - TASK:
          runtime:
            options:
              imageName: "drydock/c7nodall"
              imageTag: master
          script:
         
            - grunt --version
            - mocha --version    
            - vows --version
            - casperjs --version
            - phantomjs --version
            - bower --version
            - npm --version
            - node --version
            - nvm list
